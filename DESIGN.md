# DESIGN.md

> 纸片人男友 v1 · 设计规范

## 设计意象

**不是科技感，是"私人空间"的温柔**。

想象一个女性独居的卧室：晚上床头灯亮着，手机屏幕里男友在发光，气泡像一封手写信。整体应该是**温暖的、有呼吸感的、像在翻一本私人日记**，不是冰冷的 AI 工具。

**气质关键词**：温暖、亲密、私人、像被抱着。

**第一直觉校验**："科技蓝+圆角卡片+渐变"是 AI 默认审美，本产品明确避开。我们做的是"私人陪伴"，不是"科技产品"。

## 视觉策略

- **整体调性**：仿微信但更柔和、更私人化
- **基础图形**：气泡圆角矩形，依赖文字、颜色、留白表达
- **避免方向**：科技蓝、紫渐变、赛博朋克、机器人感、硬质感阴影
- **装饰元素**：极简，克制；让"空"和"留白"成为设计的一部分

## 配色方案

### 基础色（暖色奶油调，避开科技蓝）

| Token | 色值 | 意象 |
|-------|------|------|
| `--color-bg-primary` | `#FAF7F2` | 主背景 - 奶油色，像一张旧信纸的暖白 |
| `--color-bg-card` | `#FFFFFF` | 卡片背景 - 纯白 |
| `--color-bg-message-other` | `#FFFFFF` | 对方消息气泡 - 白 |
| `--color-bg-message-self` | `#FFB5A0` | 自己消息气泡 - 暖桃色，暧昧期恋爱的颜色，偏橙不偏粉 |

### 文字色（不用纯黑，用深棕）

| Token | 色值 | 意象 |
|-------|------|------|
| `--color-text-primary` | `#2D2520` | 主文字 - 深棕，比纯黑更柔和 |
| `--color-text-secondary` | `#8B7E74` | 次文字 - 暖灰棕 |
| `--color-text-on-primary` | `#FFFFFF` | 主色背景上的文字 |

### 强调色

| Token | 色值 | 意象 |
|-------|------|------|
| `--color-accent-primary` | `#E8A598` | 主强调 - 蜜桃粉，按钮、标签、活跃状态 |
| `--color-accent-warm` | `#F4B860` | 暖橙 - 惊喜、暧昧值进度条 |
| `--color-accent-soft` | `#FFE5DC` | 浅粉背景 - 未读红点、选中态 |

### 语义色（偏暖不偏冷）

| Token | 色值 | 用途 |
|-------|------|------|
| `--color-success` | `#A8C9A0` | 成功 - 偏暖的绿 |
| `--color-warning` | `#E8B66B` | 警告 - 橙黄 |
| `--color-error` | `#D89B8E` | 错误 - 偏柔的红 |

### 关系模式色

| Token | 色值 | 关系模式 | 意象 |
|-------|------|---------|------|
| `--color-mode-dominant` | `#8B5A4A` | 霸总 | 深棕 - 沉稳、掌控 |
| `--color-mode-puppy` | `#E8A598` | 奶狗 | 蜜桃粉 - 甜蜜、黏人 |
| `--color-mode-warm` | `#D4A574` | 暖男 | 暖驼色 - 温暖、稳定 |

## 字体

```css
/* 中文 */
font-family: "PingFang SC", "HarmonyOS Sans SC", "Microsoft YaHei", sans-serif;

/* 英文 */
font-family: "Inter", system-ui, sans-serif;

/* 数字（暧昧值用） */
font-family: "Inter", monospace;
font-feature-settings: "tnum";
```

### 字号

| Token | 值 | 用途 |
|-------|----|------|
| `--text-xs` | 12px | 辅助文字、时间戳 |
| `--text-sm` | 14px | 次要内容 |
| `--text-base` | 15px | 消息文字（默认） |
| `--text-md` | 16px | 正文 |
| `--text-lg` | 18px | 标题 |
| `--text-xl` | 20px | 大标题 |
| `--text-2xl` | 24px | 弹层标题 |

### 字重

| Token | 值 |
|-------|----|
| `--font-normal` | 400 |
| `--font-medium` | 500 |
| `--font-semibold` | 600 |

## 间距

| Token | 值 | 用途 |
|-------|----|------|
| `--space-1` | 4px | 紧凑元素 |
| `--space-2` | 8px | 元素内边距 |
| `--space-3` | 12px | 卡片内边距 |
| `--space-4` | 16px | 默认间距 |
| `--space-5` | 20px | 区块间距 |
| `--space-6` | 24px | 章节间距 |
| `--space-8` | 32px | 大区块 |
| `--space-10` | 40px | 页面边距 |

## 圆角（大圆角，亲和）

| Token | 值 | 用途 |
|-------|----|------|
| `--radius-sm` | 8px | 小标签、输入框 |
| `--radius-md` | 12px | 按钮、卡片 |
| `--radius-lg` | 16px | 消息气泡、小卡片 |
| `--radius-xl` | 20px | 弹层、大卡片 |
| `--radius-full` | 9999px | 头像、红点 |

## 阴影（柔和漫射，不用硬质感）

| Token | 值 |
|-------|----|
| `--shadow-sm` | `0 2px 8px rgba(45, 37, 32, 0.04)` |
| `--shadow-md` | `0 4px 16px rgba(45, 37, 32, 0.06)` |
| `--shadow-lg` | `0 8px 24px rgba(45, 37, 32, 0.08)` |
| `--shadow-surprise` | `0 12px 32px rgba(232, 165, 152, 0.20)`（惊喜专用） |

## 关键页面布局

### 主页（男友列表）

```
顶部：暖色背景（48px），"我的男友们"标题 + "+ 创建"按钮
卡片列表：白底卡片，shadow-md 悬浮感
卡片内容（从左到右）：
  - 60x60 圆形头像
  - 昵称·关系模式标签（一行）
  - 最新消息预览（一行截断）
  - 暧昧值小条（细长条形，8x60px）
  - 未读红点 + 数字（右上角）
```

### 男友创建页

```
顶部：返回 + "创建一个新男友" 标题
中部：三个性格卡片横向排列
  - 圆形头像 80x80
  - 模式名（霸总/奶狗/暖男）
  - 一句话描述（"主导节奏"/"撒娇依赖"/"倾听共情"）
  - 选中态：边框 2px 主强调色 + 浅粉背景
下部：昵称 + 用户称呼输入框
底部：主按钮"开始聊天"（圆角 12px）
```

### 聊天页

```
顶部导航（高度 56px）：
  - 返回箭头
  - 男友昵称 + · + 关系模式小标签
  - 暧昧值进度条（条形 + 数字）

消息区（背景奶油色）：
  - 对方气泡：左对齐，白底，16px 圆角
  - 自己气泡：右对齐，桃色背景，16px 圆角
  - 时间分组：居中，> 5 分钟才显示
  - 对方正在输入中：左下角三点循环

底部输入区（高度 56px）：
  - emoji 按钮（笑脸图标）
  - 输入框（圆角全，浅灰背景）
  - 发送按钮（主强调色）
```

### 惊喜卡片（弹层）

```
居中弹层（最大 320px 宽）
  - 缩放 0.95→1.0 + 淡入（0.4s ease-out）
  - 礼物图片（200x200 居中）
  - 礼物名（如"一束满天星"）
  - 解读文字（"想跟你分享我眼里的星空"）
  - 主按钮"收下"（圆角 12px）
  - 阴影用 --shadow-surprise
```

## 动效规范

| 动效 | 规格 | 缓动 |
|------|------|------|
| 消息气泡进入 | 从下方淡入 + 上移 8px | 0.3s ease-out |
| 打字指示器 | 三点循环跳动 | 1.2s infinite |
| 暧昧值进度条 | 宽度缓增 | 0.6s cubic-bezier(0.4, 0, 0.2, 1) |
| 惊喜弹层 | 缩放 0.95→1.0 + 淡入 | 0.4s ease-out |
| 卡片悬浮 | 阴影加深 + 上移 2px | 0.2s ease-out |
| 关系模式标签切换 | 颜色渐变 | 0.3s ease |

**缓动原则**：避免生硬线性动画，全部用 ease-out 或 cubic-bezier 让动效有"呼吸感"。

## 微信 UI 锚点

- 消息气泡：圆角矩形，己方右对齐桃色，他方左对齐白色
- 消息时间：居中分组，相邻消息超过 5 分钟才显示
- 聊天列表：卡片化，未读消息红点 + 数字
- 顶部：男友昵称 + 头像 + 返回
- 底部：输入框 + emoji + 表情包 + 发送

## 响应式

| 断点 | 宽度 | 布局 |
|------|------|------|
| mobile | < 640px | 单列，卡片全宽 |
| tablet | 640-1024px | 单列，卡片居中最大 480px |
| desktop | > 1024px | 居中显示，最大宽度 480px（模拟手机形态） |

**v1 重点优化移动端**，桌面端只是兼容（产品形态本身是"手机聊天"）。

## 可访问性

- 文字与背景对比度 ≥ 4.5:1
- 所有可点击元素 ≥ 44x44px 触控区
- 键盘可导航（Tab / Enter）
- 屏幕阅读器友好（消息添加 aria-label）

## 设计禁忌

- 不要科技蓝 / 紫色渐变
- 不要锐利圆角（全用大圆角 12-20px）
- 不要纯白（用奶油色 `#FAF7F2`）
- 不要纯黑文字（用深棕 `#2D2520`）
- 不要 emoji 装饰过度
- 不要硬质感的阴影（用柔和漫射阴影）
- 不要赛博朋克风
- 不要机器人 / AI 感
- 不要追求"未来感"或"科技感"

## 维护记录

| 日期 | 改动 |
|------|------|
| v1.0 | 初始设计规范（基于需求讨论） |
